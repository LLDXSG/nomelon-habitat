const placeList=document.querySelector('#place-list'),mapPins=document.querySelector('#map-pins'),placeForm=document.querySelector('#add-place-form'),placeCover=document.querySelector('#add-place-form [name="cover"]'),placeCoverStatus=document.querySelector('#place-cover-status'),placeStatus=document.querySelector('#place-form-status'),placeSubmit=document.querySelector('#place-submit'),placeCancel=document.querySelector('#cancel-place-edit'),placeKicker=document.querySelector('#place-form-kicker');let editingPlace=null;const seedPlaces=[{id:'place-1',title:'Japan',region:'East Asia',details:'Tokyo · Kyoto · Hokkaido',thought:'A place I want to know through its books, films, quiet streets, and ordinary rituals.',cover:''},{id:'place-2',title:'Italy',region:'Southern Europe',details:'Rome · Florence · Naples',thought:'Art, meals that last for hours, and streets that keep history close to the surface.',cover:''},{id:'place-3',title:'Iceland',region:'North Atlantic',details:'Reykjavík · Snæfellsnes',thought:'A landscape that feels like weather, music, and silence all at once.',cover:''}];let atlasPlaces=JSON.parse(localStorage.getItem('nomelon-places')||'null')||seedPlaces;const savePlaces=()=>localStorage.setItem('nomelon-places',JSON.stringify(atlasPlaces));
const setPlaceFeature=place=>{document.querySelector('#place-label').textContent='Selected from my atlas';document.querySelector('#place-title').textContent=place.title;document.querySelector('#place-meta').textContent=place.details||place.region;document.querySelector('#place-thought').textContent=`“${place.thought}”`};
const renderMap=()=>{mapPins.innerHTML='';atlasPlaces.forEach(place=>{const pin=document.createElement('button');pin.type='button';pin.textContent=place.title;pin.title=`Open note for ${place.title}`;pin.onclick=()=>setPlaceFeature(place);mapPins.append(pin)})};
const startPlaceEdit=place=>{editingPlace=place.id;placeForm.elements.title.value=place.title;placeForm.elements.region.value=place.region||'';placeForm.elements.details.value=place.details||'';placeForm.elements.thought.value=place.thought;placeCoverStatus.textContent=place.cover?'Current image will be kept unless you choose a new one.':'No image selected — a typographic place card will be created.';placeSubmit.innerHTML='Save changes <span>↑</span>';placeKicker.textContent='Edit this place';placeCancel.hidden=false;placeForm.scrollIntoView({behavior:'smooth',block:'center'})};
const renderPlaces=()=>{placeList.innerHTML='';atlasPlaces.forEach((place,index)=>{const entry=document.createElement('article');entry.className='place-entry';entry.innerHTML=`<button class="place-toggle" type="button" aria-expanded="false"><span>${String(index+1).padStart(2,'0')}</span><strong></strong><small></small><i>+</i></button><div class="place-thought"><p></p></div><div class="place-actions"><button type="button" class="edit-place">Edit</button><button type="button" class="delete-place">Delete</button></div>`;entry.querySelector('strong').textContent=place.title;entry.querySelector('small').textContent=place.region;entry.querySelector('.place-thought p').textContent=`“${place.thought}”`;entry.querySelector('.place-toggle').onclick=()=>{const open=entry.classList.toggle('open');entry.querySelector('.place-toggle').setAttribute('aria-expanded',open);setPlaceFeature(place)};entry.querySelector('.edit-place').onclick=()=>startPlaceEdit(place);entry.querySelector('.delete-place').onclick=()=>{atlasPlaces=atlasPlaces.filter(item=>item.id!==place.id);savePlaces();renderPlaces();placeStatus.textContent=`${place.title} has been removed from your private atlas.`};placeList.append(entry)});renderMap()};renderPlaces();
const resetPlaceForm=()=>{editingPlace=null;placeForm.reset();placeCoverStatus.textContent='No image selected — a typographic place card will be created.';placeSubmit.innerHTML='Add to my atlas <span>↑</span>';placeKicker.textContent='Add to the atlas';placeCancel.hidden=true};placeCancel.addEventListener('click',resetPlaceForm);placeCover.addEventListener('change',()=>placeCoverStatus.textContent=placeCover.files[0]?`Image selected: ${placeCover.files[0].name}`:'No image selected — a typographic place card will be created.');placeForm.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(placeForm),existing=atlasPlaces.find(place=>place.id===editingPlace),place={id:editingPlace||crypto.randomUUID(),title:data.get('title').trim(),region:data.get('region').trim(),details:data.get('details').trim(),thought:data.get('thought').trim(),cover:existing?.cover||''};const finish=()=>{if(editingPlace){atlasPlaces=atlasPlaces.map(item=>item.id===editingPlace?place:item);placeStatus.textContent=`${place.title} has been updated.`}else{atlasPlaces.push(place);placeStatus.textContent=`${place.title} has been added to your private atlas.`}savePlaces();renderPlaces();setPlaceFeature(place);resetPlaceForm()};const file=placeCover.files[0];if(file){if(file.size>900000){placeStatus.textContent='Please choose an image smaller than 900 KB for this browser-only atlas.';return}const reader=new FileReader();reader.onload=()=>{place.cover=reader.result;finish()};reader.readAsDataURL(file)}else finish()});
