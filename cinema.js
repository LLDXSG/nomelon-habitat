const filmList=document.querySelector('#film-list'),filmForm=document.querySelector('#add-film-form'),filmCover=document.querySelector('#add-film-form [name="cover"]'),filmCoverStatus=document.querySelector('#film-cover-status'),filmStatus=document.querySelector('#film-form-status'),filmSubmit=document.querySelector('#film-submit'),filmCancel=document.querySelector('#cancel-film-edit'),filmKicker=document.querySelector('#film-form-kicker');let editingFilm=null;const seedFilms=[{id:'film-1',title:'Perfect Days',director:'Wim Wenders',year:'2023',thought:'There is something beautiful about a life that does not need to be extraordinary to be full.',cover:''},{id:'film-2',title:'Before Sunrise',director:'Richard Linklater',year:'1995',thought:'A conversation that makes an ordinary night feel briefly infinite.',cover:''},{id:'film-3',title:'In the Mood for Love',director:'Wong Kar-wai',year:'2000',thought:'Every glance is carrying all the words the characters cannot say.',cover:''}];let films=JSON.parse(localStorage.getItem('nomelon-films')||'null')||seedFilms;const saveFilms=()=>localStorage.setItem('nomelon-films',JSON.stringify(films));
const setFilmFeature=film=>{document.querySelector('#film-label').textContent='Selected from my cinema';document.querySelector('#film-title').textContent=film.title;document.querySelector('#film-meta').textContent=[film.director,film.year].filter(Boolean).join(' · ');document.querySelector('#film-thought').textContent=`“${film.thought}”`;const poster=document.querySelector('#featured-poster');document.querySelector('#poster-year').textContent=film.year||'FILM';document.querySelector('#poster-title').innerHTML=film.title.replace(' ','<br/>');document.querySelector('#poster-director').textContent=film.director.toUpperCase();if(film.cover){poster.classList.add('has-image');poster.style.backgroundImage=`url('${film.cover}')`}else{poster.classList.remove('has-image');poster.style.backgroundImage=''}};
const startFilmEdit=film=>{editingFilm=film.id;filmForm.elements.title.value=film.title;filmForm.elements.director.value=film.director;filmForm.elements.year.value=film.year||'';filmForm.elements.thought.value=film.thought;filmCoverStatus.textContent=film.cover?'Current poster will be kept unless you choose a new one.':'No poster selected — a typographic poster will be created.';filmSubmit.innerHTML='Save changes <span>↑</span>';filmKicker.textContent='Edit this film';filmCancel.hidden=false;filmForm.scrollIntoView({behavior:'smooth',block:'center'})};
const renderFilms=()=>{filmList.innerHTML='';films.forEach((film,index)=>{const entry=document.createElement('article');entry.className='film-entry';entry.innerHTML=`<button class="film-toggle" type="button" aria-expanded="false"><span>${String(index+1).padStart(2,'0')}</span><strong></strong><small></small><i>+</i></button><div class="film-thought"><p></p></div><div class="film-actions"><button type="button" class="edit-film">Edit</button><button type="button" class="delete-film">Delete</button></div>`;entry.querySelector('strong').textContent=film.title;entry.querySelector('small').textContent=[film.director,film.year].filter(Boolean).join(' · ');entry.querySelector('.film-thought p').textContent=`“${film.thought}”`;entry.querySelector('.film-toggle').onclick=()=>{const open=entry.classList.toggle('open');entry.querySelector('.film-toggle').setAttribute('aria-expanded',open);setFilmFeature(film)};entry.querySelector('.edit-film').onclick=()=>startFilmEdit(film);entry.querySelector('.delete-film').onclick=()=>{films=films.filter(item=>item.id!==film.id);saveFilms();renderFilms();filmStatus.textContent=`${film.title} has been removed from your private cinema.`};filmList.append(entry)})};renderFilms();
const resetFilmForm=()=>{editingFilm=null;filmForm.reset();filmCoverStatus.textContent='No poster selected — a typographic poster will be created.';filmSubmit.innerHTML='Add to my cinema <span>↑</span>';filmKicker.textContent='Add to the cinema';filmCancel.hidden=true};filmCancel.addEventListener('click',resetFilmForm);filmCover.addEventListener('change',()=>filmCoverStatus.textContent=filmCover.files[0]?`Poster selected: ${filmCover.files[0].name}`:'No poster selected — a typographic poster will be created.');filmForm.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(filmForm),existing=films.find(film=>film.id===editingFilm),film={id:editingFilm||crypto.randomUUID(),title:data.get('title').trim(),director:data.get('director').trim(),year:data.get('year').trim(),thought:data.get('thought').trim(),cover:existing?.cover||''},file=filmCover.files[0];const finish=()=>{if(editingFilm){films=films.map(item=>item.id===editingFilm?film:item);filmStatus.textContent=`${film.title} has been updated.`}else{films.push(film);filmStatus.textContent=`${film.title} has been added to your private cinema.`}saveFilms();renderFilms();setFilmFeature(film);resetFilmForm()};if(file){if(file.size>900000){filmStatus.textContent='Please choose a poster smaller than 900 KB for this browser-only library.';return}const reader=new FileReader();reader.onload=()=>{film.cover=reader.result;finish()};reader.readAsDataURL(file)}else finish()});
